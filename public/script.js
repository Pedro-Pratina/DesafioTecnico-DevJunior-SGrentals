/*const enviar = document.querySelector('button')
enviar.addEventListener('click', enviainfo)
let dados = {nome: 'Pedro'}

async function enviainfo() {
    await fetch('http://127.0.0.1:3000/test-receber', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(dados)
        })
    console.log('opa')
}*/